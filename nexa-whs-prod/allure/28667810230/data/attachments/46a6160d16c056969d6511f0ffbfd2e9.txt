Shadow Borrower View: checkbox at index 0 is not disabled — the app may enforce read-only via CSS pointer-events instead
Shadow Borrower View: checkbox at index 2 is not disabled — the app may enforce read-only via CSS pointer-events instead
Shadow Borrower View: checkbox at index 4 is not disabled — the app may enforce read-only via CSS pointer-events instead
